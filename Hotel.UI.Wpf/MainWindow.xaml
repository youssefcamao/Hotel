<Window x:Class="Hotel.UI.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vms="clr-namespace:Hotel.UI.Wpf.MVVM.ViewModels"
        xmlns:views="clr-namespace:Hotel.UI.Wpf.MVVM.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     FontFamily="{DynamicResource MaterialDesignFont}"
        xmlns:local="clr-namespace:Hotel.UI.Wpf"
        mc:Ignorable="d"
        Title="Hotel" Height="800" Width="800"
        ResizeMode="NoResize" WindowStyle="None"
    Background="{x:Null}"
    WindowStartupLocation="CenterScreen"
        AllowsTransparency="True">
    <WindowChrome.WindowChrome>
        <WindowChrome 
        CaptionHeight="0"
        ResizeBorderThickness="5" />
    </WindowChrome.WindowChrome>
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type vms:LoginViewModel}">
                <views:LoginView Password="{Binding UserPassword, Mode=OneWayToSource}"/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:AdminViewModel}">
                <views:AdminView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:UserViewModel}">
                <views:UserView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:SignupViewModel}">
                <views:SignupView/>
            </DataTemplate>
        </Grid.Resources>
        <materialDesign:Card UniformCornerRadius="15" MouseLeftButtonDown="Card_MouseLeftButtonDown" Background="{DynamicResource MaterialDesignPaper}"  materialDesign:ShadowAssist.ShadowDepth="Depth4">
            <materialDesign:PopupBox VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0 20 20 0" PlacementMode="BottomAndAlignRightEdges" StaysOpen="False" Height="25" RenderTransformOrigin="1.088,-15.18">
                <StackPanel>
                    <StackPanel Margin="16 10 0 6" Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock  VerticalAlignment="Center" Text="Dark Mode" />
                        <ToggleButton Cursor="Hand" ToolTip="Enable Dark Mode" Margin="12 0 8 0" x:Name="themeToggle" IsChecked="{Binding IsDarkTheme}" Click="themeToggle_Click"/>
                    </StackPanel>
                    <Button x:Name="Btn_Exit" ToolTip="Close Application" Content="Exit Application" Click="Btn_ExitApp"/>
                </StackPanel>
            </materialDesign:PopupBox>
        </materialDesign:Card>
        <ContentControl Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>
